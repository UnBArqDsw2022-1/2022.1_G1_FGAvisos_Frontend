import styled, { css } from "styled-components";

const Titulo = styled.span`
  font-family: "Inter";
  font-style: normal;
  font-weight: 700;
  line-height: 36px;
  color: #000000;
`;

export default Titulo;
